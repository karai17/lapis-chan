<% if thread then %>
<form action="#" method="get">
	<button type="button" data-path="<%= post.file_path %>" id="r<%= post.post_id %>" class="remix">📼 Remix Image</button>
</form>
<% else %>
<form action="<%= post.remix %>" method="get">
	<button>📼 Remix Image</button>
</form>
<% end %>
